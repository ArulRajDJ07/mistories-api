'use strict';

/**
 * story-version-history router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::story-version-history.story-version-history');
