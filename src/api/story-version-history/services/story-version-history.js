'use strict';

/**
 * story-version-history service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::story-version-history.story-version-history');
