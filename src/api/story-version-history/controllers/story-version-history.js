'use strict';

/**
 * story-version-history controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::story-version-history.story-version-history');
